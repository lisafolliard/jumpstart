<h1>jumpst<span class='title_font'>art</span></h1>
<span class='site_about'><p>Portland artists can submit works of art to be reviewed by local art critics</p></span>

<hr>

<div class='container'>
<%= link_to "Sort by Date Added", :sort => "created_at" %>

<br>
<div class='image-main'>
    <ul>
      <% @images.each do |image| %>
        <div class='col-sm-4'>
          <li><%= link_to image_tag(image.image), user_path(image.user)  %></li>
          <li><%=  link_to image.user.username.titleize, user_path(image.user) %></li>
        </div>
      <% end %>
    </ul>
  </div>
</div>

<hr>
