<h1>jumpstart <span class='title_font'>Portland</span></h1>
<span class='site_about'><p>Submit works of art to be reviewed by experienced local art critics</p></span>

<hr>


<% @search.each do |search| %>
   <h3>Your search results:</h3>
   <%= search.username %>
<% end %>


<div class='container'>
  <li><%= link_to 'Sort by Artist Name', :sort => 'user_id' %></li>
  <br>
  <div class='image-main'>
    <ul>
      <% @images.each do |image| %>
        <div class='col-sm-4'>
          <li><%= link_to image_tag(image.image), user_path(image.user)  %></li>
          <li><%=  link_to image.user.username.titleize, user_path(image.user) %></li>
        </div>
      <% end %>
    </ul>
  </div>
</div>




<hr>
